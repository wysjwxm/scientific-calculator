# 项目规则

本文件是**本项目的工作规则**（怎么做），设计决策与技术约定见 `docs/superpowers/specs/` 下的 spec，spec 是设计层面的权威。

## 单元测试一律委派给 subagent 实现

**所有单元测试的新增、修改与断言调整，都由 subagent 实现，主 session 不直接改测试文件。**

具体执行方式：

- 测试与实现放在**同一次实现派发**里：brief 携带完整的测试代码，实现者按 TDD 先跑红、再写实现跑绿。
- 修正轮（fix round）里需要动断言的，同样走 subagent；主 session 只裁定「该不该改、改成什么」，不落手改。
- 测试的评审与复核由**另一个** subagent 承担；实现者自己派生 reviewer 是不允许的。
- 主 session 的职责是：拆解任务、写 brief、裁定冲突、审阅报告与评审结论。

## 技术约束

- Java 17 + Spring Boot 3.5.x + Maven（`mvn`，仓库里没有 `mvnw`）+ JUnit 5 & AssertJ。
- **不引入任何第三方依赖**：不加数据库、不加缓存、不加外部接口，全部内存实现。
- `domain` 包**不 import 任何 `org.springframework.*`** —— 领域层不认识框架。
- 注释与错误信息用中文，风格与周围代码保持一致；命名清晰、结构直观，代码是给人读的。
- 交付物是可直接 `java -jar` 运行的独立 jar，不依赖任何中间件。

## 环境纪律

- 不安装、不下载任何软件包或命令行工具，不修改系统级配置。
- 除明确授权的验收步骤外，不启动服务或后台进程；排查用的探针放 `/tmp`，不放进仓库。
